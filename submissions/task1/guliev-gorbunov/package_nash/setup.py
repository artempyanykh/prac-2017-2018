from distutils.core import setup

setup(
    name = 'package_nash',
    version = '0.1dev',
    packages = ['package_nash',],
    license = 'MIT',
    long_descriptions = open('README.txt').read(),
)
